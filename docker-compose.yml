version: "3.9"
services:
  dumdumbackend:
    image: quay.io/kurama/jwc-backend:dumdum
    restart: always
    labels:
      - traefik.http.routers.admin.rule=Host(`dumdum-backend.mamun.cloud`)
      - traefik.http.routers.admin.tls=true
      - traefik.http.routers.admin.tls.certresolver=lets-encrypt
      - traefik.port=8001
    networks:
     - internal
     - web
    ports:
      - "8001:8001"
networks:
 web:
   external: true
 internal:
   external: false
